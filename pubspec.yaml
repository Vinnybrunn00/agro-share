name: agroshare
description: "o AgroShare é um aplicativo voltado para produtores ruais de um mesma região alugarem, emprestarem e trocarem máquinas, implementos e ferramentas agrícolas ociosas."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0
  firebase_storage: ^13.5.0
  google_fonts: ^8.2.1
  flutter_email_sender: ^10.0.1
  provider: ^6.1.5+1
  iconsx_plus: ^1.0.1
  flutter_launcher_icons: ^0.14.4
  firebase_core: ^4.14.0
  mask_text_input_formatter: ^2.9.0
  image_picker: ^1.2.3
  shimmer: ^4.0.0
  geolocator: ^14.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/icon.png"
  min_sdk_android: 23
  remove_alpha_ios: true
  adaptive_icon_background: "#4b9068"
  adaptive_icon_foreground: "assets/icons/icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icons/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
